## Server
server.port=${SERVER_PORT:${PORT:8080}}

## MongoDB (prefer Spring env names, fall back to legacy, and finally to a safe local default)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:${MONGODB_URI:mongodb://localhost:27017/job_portal_db}}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:${MONGODB_DATABASE:job_portal_db}}

## CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:${ALLOWED_ORIGINS:*}}

## Actuator (health used by ALB)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.health.defaults.enabled=true
management.endpoint.health.show-details=never